import { Container, Typography } from "@mui/material";
import React from "react";

const Services = () => {
  return (
    <Container sx={{ pt: 2 }}>
      <Typography>Préstations</Typography>
    </Container>
  );
};

export default Services;
